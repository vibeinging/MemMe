# MemMe

> Edge-first AI memory engine. Rust-native, single-file DuckDB storage, pluggable LLM, offline-first. LoCoMo benchmark: 82.92% (outperforms mem0, Zep).

## Core Concepts

- **Single-file DuckDB storage**: One `.duckdb` file holds vectors, knowledge graph, FTS index, and change history. No external databases.
- **Pluggable LLM**: Works without LLM (<10ms vector dedup) or with any LLM (Ollama, OpenAI, Anthropic, Gemini) for fact extraction.
- **Session/Episode architecture**: Raw events → Sessions → Episodes (LLM purification) → Atomic memory traces → Identity traits.
- **4-channel RRF retrieval**: Vector similarity + BM25 FTS + Entity spreading activation + Temporal search, fused via adaptive Reciprocal Rank Fusion + optional cross-encoder reranking.
- **FSRS forgetting curve**: Power-law memory decay `R(t,S) = (1 + t/(c·S))^(-p)` with stability reinforcement on access.
- **Meditation**: 4-phase memory consolidation — decay, per-episode fact extraction, vector dedup (cosine similarity, no LLM needed), graph + entity linking.

## Workspace Crates (9)

- `memme-core` — Core engine: CRUD, search, graph, dedup, meditation, forgetting curve, backup, export/import, analytics
- `memme-embeddings` — Embedding trait + ONNX/OpenAI/Ollama backends
- `memme-llm` — LLM trait + Ollama/OpenAI/Anthropic/Gemini backends for fact extraction
- `memme-python` — Python bindings via PyO3 + maturin
- `memme-ffi` — Swift/Kotlin/C bindings via UniFFI
- `memme-node` — Node.js bindings via NAPI-RS
- `memme-wasm` — WASM bindings via wasm-bindgen
- `memme-server` — REST API server (axum) with Bearer auth, CORS
- `memme-mcp` — MCP stdio server for Claude Desktop / Cursor

## Key API Methods

- `add(content, AddOptions)` — Add a memory with automatic vector dedup
- `search(query, SearchOptions)` — 4-channel retrieval with RRF fusion
- `get_trace(id)` / `update_trace(id, content)` / `delete_trace(id)` — CRUD
- `list_traces(ListOptions)` — List with filters
- `append_events(session_id, messages, user_id)` — Primary ingestion
- `compact(session_id)` — Purify events, create Episode
- `meditate(user_id)` — 4-phase consolidation: decay → extract → dedup → graph
- `reflect(user_id)` — LLM-powered analysis of recent memories for themes/patterns
- `learn_from_feedback(user_id, feedback)` — Extract behavioral principles from corrections
- `diagnose()` — Health checks for storage, embedder, LLM with latency
- `backup_to_path(path)` / `restore_from_backup(path)` — Portable backup/restore
- `sync_replica()` — Dual-replica protection
- `full_export(user_id)` / `full_import(data)` — Complete JSON export/import
- `import_conversations(convs, user_id)` — Import ChatGPT/Claude/Gemini history

## Security

- Memory immutability (lock memories from modification/deletion)
- Privacy levels: LocalOnly / Syncable / EncryptedSync per-memory
- Data expiration (TTL) with automatic cleanup
- Audit trail (history table logs every change)
- Bearer token auth + CORS on REST server
- Exclusion prompts (tell LLM what not to extract)
- Parameterized SQL queries + collection name whitelist
- Webhook notifications (feature-gated)

## Multi-Platform Bindings

Python (PyO3) / Node.js (NAPI-RS) / Swift & Kotlin (UniFFI) / WASM (wasm-bindgen)

## Links

- Website: https://vibeinging.github.io/MemMe/
- GitHub: https://github.com/vibeinging/MemMe
- Crates.io: https://crates.io/crates/memme-core
- License: Apache-2.0
