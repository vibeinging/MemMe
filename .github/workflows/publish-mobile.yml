name: Mobile publication paused

on:
  workflow_dispatch:

jobs:
  blocked-until-vexdb-static-registration:
    name: VexDB-Lite static registration required
    runs-on: ubuntu-latest
    steps:
      - name: Explain publication boundary
        run: |
          echo "::error::Mobile publication is disabled until VexDB-Lite is statically registered with MemMe's SQLite build and verified on real iOS and Android targets."
          exit 1
